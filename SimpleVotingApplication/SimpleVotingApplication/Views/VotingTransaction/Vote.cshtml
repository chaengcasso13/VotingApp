@*@model IEnumerable<SimpleVotingApplication.Models.CandidateModel>*@

@{
    ViewData["Title"] = "Vote";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Vote</h1>
<div class="row">
    <div class="col-md-4">
        <form asp-action="CastVote">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                Select Candidate:

            @*@for(int i = 1; i <= ViewBag.Candidates.Count; i++)
            {
                    {
                        var candidateModel = ViewBag.Candidates;
                        <select name="candidateModel.CandidateID" class="form-select" aria-label="Default select example">
                            foreach (var candidate in candidateModel)
                            {
                                <option value="@candidateModel.CandidateID">@candidateModel.FullName</option>
                            }
                        </select>
                    }
            }*@
            <select name="candidateID" class="form-select" aria-label="Default select example">
                @foreach (var candidate in ViewBag.Candidates)
                {
                <option value="@candidate.CandidateID">@candidate.FullName</option>
                }
            </select>
            
            <div class="form-group">
                <input type="submit" value="Submit Vote" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
